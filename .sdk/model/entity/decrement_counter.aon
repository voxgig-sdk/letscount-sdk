# Entity: decrement_counter

main: kit: entity: decrement_counter: {

  alias: field: {}

  fields: [
    {
      name: "id"
      req: false
      type: "`$STRING`"
    }
  ]

  id: {
    field: "id"
    name: "id"
    parts: [
      "namespace"
      "key"
    ]
    sep: "/"
  }

  name: "decrement_counter"
  op: {
    remove: {
      name: "remove"
      points: [
        {
          args: {
            params: [
              {
                kind: "param"
                name: "key"
                orig: "key"
                reqd: true
                type: "`$STRING`"
              }
              {
                kind: "param"
                name: "namespace"
                orig: "namespace"
                reqd: true
                type: "`$STRING`"
              }
            ]
          }
          method: "DELETE"
          orig: "/{namespace}/{key}"
          segments: [
            {
              var: "namespace"
            }
            {
              var: "key"
            }
          ]
          select: {
            exist: [
              "key"
              "namespace"
            ]
          }
          transform: {
            req: "`reqdata`"
            res: "`body`"
          }
        }
      ]
    }
  }



}
